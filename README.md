# CityLensAI

Hybrid stack: .NET Core (API & gateway), Angular (frontend), Python FastAPI (AI microservices).
This repo contains scaffolding and infra for CityLens AI—a multimodal urban intelligence platform.

## Quickstart
1. Install prerequisites (dotnet, node, python, docker)
2. Run `./scripts/setup-local.sh` (will be added)
